Rama sugerida:
feature/refactor-crear-reserva

Secuencia de commits semánticos:

1. chore(reservas): crear estructura inicial por capas
2. feat(domain): implementar ReservaBuilder con interfaz fluida
3. feat(infra): agregar NotificadorFactory configurable por entorno
4. feat(application): crear ReservaService transaccional
5. refactor(web): reducir la vista de creación de reserva
6. test(reservas): cubrir disponibilidad y pertenencia de servicios
7. docs(wiki): documentar patrones Builder y Factory

Flujo recomendado:

git checkout develop
git pull
git checkout -b feature/refactor-crear-reserva

git push -u origin feature/refactor-crear-reserva

# Crear Pull Request hacia develop.
# Solicitar revisión de otro integrante.
# Hacer merge cuando las pruebas estén en verde.
